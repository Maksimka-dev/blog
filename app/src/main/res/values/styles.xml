<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryTransparent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryTransparent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LightTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary_</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark_</item>
        <item name="android:colorAccent">@color/colorAccent_</item>
        <item name="android:fontFamily">@font/roboto_slab</item>
        <item name="android:textColorHint">@color/textColor_</item>
        <item name="android:editTextColor">@color/textColor_</item>
        <item name="android:textColorHighlight">@color/colorBackground_</item>
        <item name="android:textColor">@color/textColor_</item>
        <item name="buttonStyle">@style/buttonLight</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="buttonLight" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:background">@drawable/round_shape</item>
        <item name="android:textSize">@dimen/font_big</item>
        <item name="android:textColor">@color/colorPrimary_</item>
    </style>

    <style name="DarkTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:fontFamily">@font/roboto_slab</item>
        <item name="android:textColorHint">@color/textColor</item>
        <item name="android:editTextColor">@color/textColor</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="buttonStyle">@style/buttonLight</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition" >
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="buttonDark" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="background">@drawable/round_shape</item>
        <item name="android:textSize">@dimen/font_big</item>
        <item name="textColor">@color/textColor</item>
    </style>

    <style name="dividerStyle" parent="LightTheme">
        <item name="backgroundTint">@color/colorBlack</item>
    </style>

    <style name="previewText" parent="LightTheme">
        <item name="android:textSize">@dimen/font_regular</item>
        <item name="android:textColor">@color/colorPrimaryTransparent</item>
    </style>

    <style name="messageText" parent="LightTheme">
        <item name="android:textSize">@dimen/font_regular</item>
    </style>

    <style name="fabStyle" parent="">
    </style>
</resources>
